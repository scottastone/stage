[project]
name = "stage"
version = "0.1.0"
description = "Direct file transfer between machines via Tailscale or direct connection"
requires-python = ">=3.14"
dependencies = []

[project.scripts]
stage = "stage:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
